package worq.order.app

/**
 * Application-scoped dependency boundary.
 *
 * Dependencies are intentionally absent until their implementation milestones.
 */
interface ApplicationContainer

internal class DefaultApplicationContainer : ApplicationContainer
